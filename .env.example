# Copy to .env and fill in. .env is git-ignored.
# In GitLab CI these are set as CI/CD variables instead, no .env file is used.

# Name of the Azure Container Registry, without the .azurecr.io suffix
ACR_NAME=

# Repository inside the registry
IMAGE_NAME=microservices-app
